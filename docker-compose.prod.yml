version: '3.8'

services:
  backend:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: unichat-backend
    ports:
      - "3000:3000"
    env_file:
      - backend/.env
    restart: always

